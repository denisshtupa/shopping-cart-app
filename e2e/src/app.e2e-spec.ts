import { AppPage } from './app.po';
import { browser, logging } from 'protractor';

describe('shopping-cart-app App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  it('test title of app', () => {
  });
});
